# SQL queries (hot-reloadable). Edit and save to apply without restart.

# Database connection (loaded at startup)
db.service = 23ai_34ui2
db.username = RIZCKY
db.password = 3JHRJRG
db.server = db.freesql.com
db.port = 1521
db.timezone = +00:00

# Optional: database/sql pool tuning
db.pool.max_open = 20
db.pool.max_idle = 10
db.pool.conn_max_lifetime = 30m
db.pool.conn_max_idletime = 5m

# Optional: godror driver session pooling (uncomment to enable)
# Use 0 to enable session pool (recommended in many cases), 1 for standalone connections
# db.godror.standalone_connection = 0
# db.godror.pool_min_sessions = 1
# db.godror.pool_max_sessions = 20
# db.godror.pool_increment = 1

# List all products
listProducts = SELECT PRODUCT_ID, NAME, ATTRIBUTES_VARCHAR, ATTRIBUTES_CLOB, ATTRIBUTES_BLOB FROM PRODUCTS ORDER BY PRODUCT_ID

# Get single product by ID
getProduct = SELECT NAME, ATTRIBUTES_VARCHAR, ATTRIBUTES_CLOB, ATTRIBUTES_BLOB FROM PRODUCTS WHERE PRODUCT_ID = :1

# Compute next product ID
nextProductId = SELECT NVL(MAX(PRODUCT_ID), 0) + 1 FROM PRODUCTS

# Insert a product
insertProduct = INSERT INTO PRODUCTS (PRODUCT_ID, NAME, ATTRIBUTES_VARCHAR, ATTRIBUTES_CLOB, ATTRIBUTES_BLOB) VALUES (:1, :2, :3, :4, :5)

# Update a product
updateProduct = UPDATE PRODUCTS SET NAME = :1, ATTRIBUTES_VARCHAR = :2, ATTRIBUTES_CLOB = :3, ATTRIBUTES_BLOB = :4 WHERE PRODUCT_ID = :5

# Delete a product
deleteProduct = DELETE FROM PRODUCTS WHERE PRODUCT_ID = :1
